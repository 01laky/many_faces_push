# `internal/`

Future home for gRPC service implementation, FCM client adapters, and retry/redaction helpers.

**Rule:** no face-scoped business rules or PostgreSQL access here — those stay in **`many_faces_backend`**.
