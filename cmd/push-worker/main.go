// Command push-worker is the future FCM / gRPC sidecar for Many Faces (many_faces_push).
// Skeleton only: no gRPC listener and no Firebase wiring yet — see repository README and monorepo agent prompt.
package main

import (
	"log"
	"os"
)

func main() {
	log.Println("many_faces_push: skeleton — push worker not implemented yet")
	os.Exit(0)
}
